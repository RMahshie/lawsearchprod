# Backend Environment Variables
# Copy this file to .env and update the values

# === API Configuration ===
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=true

# === Application Settings ===
ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=INFO

# === OpenAI Configuration ===
# REQUIRED: Get your API key from https://platform.openai.com/account/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# === Model Configuration ===
LLM_INGEST=gpt-4o
LLM_QA=gpt-4o
EMBEDDING_MODEL=text-embedding-3-small

# === RAG Configuration ===
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
TOP_K_RETRIEVAL=8
SIMILARITY_THRESHOLD=0.7

# === Directory Paths ===
# These will be auto-set relative to project root if not specified
# BASE_DIR=/path/to/project
# DATA_DIR=/path/to/data/bills
# VECTORSTORE_DIR=/path/to/db/chroma

# === CORS Configuration ===
CORS_ORIGINS=["http://localhost:3000","http://localhost:5173","http://localhost:5174"]
CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_METHODS=["*"]
CORS_ALLOW_HEADERS=["*"]

# === Security Settings ===
# Add any additional security settings here
SECRET_KEY=your-secret-key-for-sessions